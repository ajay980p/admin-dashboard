export type Credentials = {
    email: string
    password: string
}

export type UserData = {
    firstName: string
    lastName: string
    email: string
    password: string,
    role: string
}


export type TenantData = {
    name: string
    mailId: string
    address: string
}